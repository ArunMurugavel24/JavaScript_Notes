var fullName = 'Arun';

// how to name variable: - stay consistent
// fullName (camecase - mostly used)
// full-name
// full_name
// FullName (not used mostly)

var courseName = 'React JS BootCamp'; //strings datatype

var isLoggedIn = true; //boolean datatype

var loggedCount = 34; // int datatype

var paymentMode; // for Dynamic Initialization
console.log(paymentMode); // Since no value - Undefined Datatype

paymentMode = 'Credit Card';
console.log(paymentMode); // now it is assignes a "string" Datatype

console.log(loggedCount); // references variable name
console.log('loggedCount'); // Just a string to be printed/logged

// cannot use reserved keywords for variables.
